import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme)=>({
    PostContainer : {
        background: '#1b1b1b',
        direction:"column",
        alignItems:"center",
        justifyContent:"center",
    },
}))