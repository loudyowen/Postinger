import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme)=>({
    PostContainer : {
        background: '#7EC8E3',
        direction:"column",
        alignItems:"center",
        justifyContent:"center",
    },
    grid: {
    }
}))